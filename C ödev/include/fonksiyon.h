#ifndef FONKSIYON_H
#define FONKSIYON_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char  **ft_split(char const *str, char c);
char	*ft_substr(char const *s, unsigned int start, size_t len);

#endif
